FROM rust:1.84-bookworm

# Install Debian packages required for Tauri 2 Linux builds + Node + Go
RUN apt-get update -qq && \
    apt-get install -y --no-install-recommends \
      libwebkit2gtk-4.1-dev \
      libssl-dev \
      libgtk-3-dev \
      libayatana-appindicator3-dev \
      librsvg2-dev \
      policykit-1 \
      curl \
      wget \
      file \
      build-essential \
      pkg-config \
      nodejs \
      npm \
      golang-go && \
    rm -rf /var/lib/apt/lists/*

# Install Cargo tools
RUN cargo install cargo-audit --locked || true

WORKDIR /workspace

CMD ["cargo", "--version"]
